url: http://join.techhire.org

markdown: kramdown
permalink: pretty
gems: ['kramdown']
exclude: ['config.ru', 'Gemfile', 'Gemfile.lock', 'vendor', 'Procfile', 'Rakefile']

collections:
  - plays

sass:
    sass_dir: assets/_sass